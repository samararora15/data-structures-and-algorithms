int c=0;
    // while(k!=NULL) {
    //     c++;
    //     k=k->next;
    // }
    // c=c/2;
    // temp = (struct node*)malloc(sizeof(struct node));
    // cout<<"ente middle node:";
    // cin>>temp->data;
    // while(c>0) {
    //     p = p->next;
    //     c--;
    // }
    // struct node *q;
    // q=p;
    // temp->next = p->next;
    // q->next = temp;